import 'package:flutter/material.dart';

class newform extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('under maintance'),
    );
  }
}
